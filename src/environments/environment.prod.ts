export const environment = {
  production: true,
  // Garanta que a URL de produção também não tenha o /api, ou tenha o que for correto para o seu servidor de produção.
  apiBaseUrl: 'http://localhost:8080' // Ajuste se necessário para o seu ambiente de produção
};